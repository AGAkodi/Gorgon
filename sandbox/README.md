# sandbox

Transaction simulation engine (Layer 1). Runs proposed calls or decoded
payloads against a decoy wallet on an EVM mainnet-fork (Tenderly/Anvil) or a
forked Solana local validator, producing the wallet impact report shape
defined in `schemas/simulation.schema.json`.

Not yet implemented — see Phase 5 in `TODO.md`.
