# static-analysis

EVM (Slither) and Solana (Soteria/Anchor) static analyzers, normalized into the
shared `static_findings` shape defined in `schemas/verdict.schema.json`.

Not yet implemented — see Phase 1 in `TODO.md`.
